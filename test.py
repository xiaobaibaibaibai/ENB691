import numpy as np
import time
import random

y = np.array([
    [1],
    [1]
])

y = np.array([
    1, 1
])

margin = np.array([
    [1, 10.5],
    [-11.5, 1]
])

# margin[np.arange(2), y] = 0
margin[[0,1]]

print(np.arange(2))
print(margin[[0,1], ])